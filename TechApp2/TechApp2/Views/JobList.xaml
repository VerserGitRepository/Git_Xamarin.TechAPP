<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TechApp2.Views.JobList">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="JOBS ASSIGNED TO USER" FontSize="17"
                VerticalOptions="CenterAndExpand" Margin="0,0,0,0"
                HorizontalOptions="CenterAndExpand" />
            <DatePicker HorizontalOptions="End"  />
            <ListView x:Name="JobListView" 
            ItemsSource="{Binding jobs}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10,0,20,0">
                                <Grid x:Name="grd">
                                    <Label Text="{Binding JobNo}" TextColor="Gray" FontSize="Small" HorizontalOptions="StartAndExpand" HeightRequest="20" />
                                    <!--<Label Text="{Binding JobStatus}" />-->
                                    <!--<Label Text="{Binding SiteName}" />
                                    <Label Text="{Binding ProjectName}" />
                                    <Label Text="{Binding JobDate}" />-->
                                </Grid>
                            </StackLayout>
                        </ViewCell>                   

                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
            <StackLayout HorizontalOptions="Start" VerticalOptions="StartAndExpand">
                <!--<Button Text="BACK" x:Name="btnback" BackgroundColor="Blue" Clicked="btnback_Clicked"   TextColor="White" HeightRequest="50" VerticalOptions="Center"></Button>-->
            </StackLayout>
        </StackLayout>
       
    </ContentPage.Content>
</ContentPage>